USE tfg_english_db;

-- MySQL schema and seed data for this project are managed by Spring Boot (JPA + DataLoader).
-- Keep this file so Docker still runs the init folder; Hibernate will create/update tables on startup.
